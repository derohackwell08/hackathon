## Packages
framer-motion | Page transitions and sleek entry animations
recharts | Dashboard analytics and results visualizations
date-fns | Formatting exam dates and study plan timelines
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
- Using Replit Auth (useAuth) from @/hooks/use-auth
- Replit AI Integrations SSE for Chat is handled using fetch reader in custom useChatStream hook
- Ensure tailwind config has font family extended for 'sans'
